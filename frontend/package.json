{
  "name": "ai-deployment-calculator-frontend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  },
  "scripts": {
    "build": "vite build",
    "dev": "vite --host 127.0.0.1",
    "preview": "vite preview --host 127.0.0.1",
    "lint": "npm --prefix ../harness run lint",
    "setup:e2e": "npm --prefix ../harness run setup:e2e",
    "test:file": "npm --prefix ../harness run test:file --",
    "test": "npm --prefix ../harness run test:coverage",
    "test:coverage": "npm --prefix ../harness run test:coverage",
    "test:e2e": "npm --prefix ../harness run test:e2e",
    "typecheck": "npm --prefix ../harness run typecheck:frontend"
  },
  "dependencies": {
    "zod": "latest"
  },
  "devDependencies": {
    "vite": "latest"
  },
  "size-limit": [
    {
      "name": "main JS",
      "path": "dist/assets/*.js",
      "limit": "17 kB"
    },
    {
      "name": "CSS",
      "path": "dist/assets/*.css",
      "limit": "6 B"
    }
  ]
}

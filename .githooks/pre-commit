#!/bin/sh
# Ralph commit preflight: fast lint/format for everyone, plus loop containment.
# Bypassing this hook is forbidden by AGENTS.md.
set -eu

.venv/bin/harness preflight

if git diff --cached --quiet; then
  echo "Empty commit rejected. Commit real work." >&2
  exit 1
fi
